// src/app/contact/page.tsx
export const metadata = {
  title: "Contact Us | Scribes Consulting",
  description: "Contact Scribes Consulting for inquiries, collaborations, or support.",
};

import ContactForm from "./ContactForm";

export default function ContactPage() {
  return <ContactForm />;
}
